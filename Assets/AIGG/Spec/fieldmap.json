{
  "englishToPath": {
    "tracks": "$.difficulty.tracks",
    "grid columns": "$.grid.cols",
    "grid rows": "$.grid.rows",
    "player speed start": "$.difficulty.playerSpeed.start",
    "player speed delta": "$.difficulty.playerSpeed.deltaPerTrack",
    "ordering": "$.progression.ordering",
    "carrier speed": "$.progression.carrier.moveSpeed",
    "jump force": "$.difficulty.jumpForce",
    "max gap": "$.difficulty.gapRules.maxGapWidth",
    "player speed": "$.difficulty.playerSpeed.start",
    "speed delta": "$.difficulty.playerSpeed.deltaPerTrack",
    "gap probability": "$.difficulty.gapProbability.start",
    "gap delta": "$.difficulty.gapProbability.deltaPerTrack",
    "gap max": "$.difficulty.gapProbability.max",
    "max gap width": "$.difficulty.gapRules.maxGapWidth",
    "grid cols": "$.grid.cols",
    "dx": "$.grid.dx",
    "dy": "$.grid.dy",
    "origin x": "$.grid.origin.x",
    "origin y": "$.grid.origin.y",
    "lanes": "$.trackTemplate.lanes",
    "length units": "$.trackTemplate.lengthUnits",
    "tile width": "$.trackTemplate.tileWidth",
    "start width": "$.trackTemplate.zones.start.size.x",
    "start height": "$.trackTemplate.zones.start.size.y",
    "end width": "$.trackTemplate.zones.end.size.x",
    "end height": "$.trackTemplate.zones.end.size.y",
    "kill y": "$.trackTemplate.killZone.y",
    "kill height": "$.trackTemplate.killZone.height",
    "carrier type": "$.progression.carrier.type",
    "attach kinematic": "$.progression.carrier.attachKinematic",
    "camera x": "$.camera.offsetX",
    "camera y": "$.camera.offsetY",
    "camera smooth": "$.camera.smooth"
  }
}